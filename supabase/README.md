# Shared Supabase migrations (platform database)

These migrations belong to the **VectorWorkspace platform**, not to a single app.
KTPHUB, future KSP editor, logic courses, and other services share the same project.

See:

- [docs/supabase/setup.md](../docs/supabase/setup.md)
- [docs/supabase/schema.md](../docs/supabase/schema.md)
- Root `.env.example` / `.env.local` for credentials
