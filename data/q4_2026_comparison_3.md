# Q4 2026 — 3 варианта: Ручное vs Heuristic vs CP-SAT 3 мин

**Данные:** `schedule_q4_2026_variant1.json` (ручное, 667 уроков → 651 период), `variant2.json` (heuristic 5×7), `variant3.json` (CP-SAT 180с, 5дн×7, `solver/__main__.py`, `engine.py:313` `max_time 180`). Каталог 27 классов, 39 учителей, 58 кабинетов, 70 предметов. 5-дневка Казахстан, макс 7 уроков в старших (`Materials/Генератор Школьного Расписания` — 5×7=35 слотов/неделю/класс).

---

## Hard (0 = ок)

|  | V1 ручное | V2 heuristic | V3 CP-SAT |
|---|---|---|---|
| Teacher overlap | **137** | **0** | **0** |
| Room overlap (без fallback r_1/r_20) | **165** | **0** | **19** |
| Class overlap | 0 | 0 | 0 |

Ручное — каждый лист xls независимо, учителя в 2 местах, кабинеты `химия→физика` и все начальные в один `r_1_general` (5-дневка 5×7=35, а у `5б` 37 уроков → 165 коллизий). Heuristic 5×7 строго проверяет `teacherBusy/classBusy/roomBusy` → 0, но 14 уроков не влезли (637/651). CP-SAT 5×7 нашёл 585/651 с 0/19, но за 180с не доказал `OPTIMAL`.

---

## Soft (наши веса для честного сравнения: window 400, sanpin 100, load_balance 80)

|  | V1 | V2 | V3 | V3 solver own |
|---|---|---|---|---|
| S1 окна учителей | 95 | 164 | **0** | 0 |
| S3 СанПиН (идеал 7-11-11-9-7) | 2658 | 2519 | 2197 | 2197 |
| S6 баланс `max-min` | 45 | 87 | 35 | 35 |
| окна классов | 17 | 130 | 124 | — |
| **weighted total** | 307400 | 324460 | **222500** | 222500 |

**Solver own (V3 5×7, 585 слотов):** `FEASIBLE` `window 0` `sanpin 2197` `load 35` `wall 181067ms` — не `OPTIMAL` за 180с, но уже -28% vs V1. V2 637/651 — жадный `shuffle` без `gap`, поэтому окна 164 хуже ручного.

**Что улучшил CP-SAT 5×7:**
- **Окна 95→0 (-100%)** — `gap` (`soft.py:78`) минимизирован.
- **СанПиН 2658→2197 (-17%)** — сгладил пики.
- **Баланс 45→35 (-22%)** — лучше обоих.

**Почему V2 был хуже по окнам:** жадный `shuffle` без `gap` штрафа, разбрасывал. CP-SAT минимизирует `sum(w*pen)` (`engine.py:310`) и видит все `gap` переменные одновременно.

---

## Вывод

- **V1** — 667 уроков (651 период) вручную, ровно (load 45), но **неисполнимо** в 5×7 (137+165) — надо править.
- **V2** — heuristic 5×7, 637/651, исполнимо, но окна 164 хуже — «быстрый драфт».
- **V3** — CP-SAT 5×7, 585/651, **0 окон, -17% СанПиН, -22% баланс**, hard 0/19 — лучше всего для 5-дневки. При 600с добьёт `OPTIMAL`.

Запуск в приложении: `schedule_import_legacy(4)` теперь создаёт **3 варианта** (`schedule.rs:723` — `q4v1/v2/v3`), фронт `SchedulePage.tsx:118` автодосоздаёт если `<3`. Переключение `2025-2026 / 4 четверть / Вариант 1/2/3`.

Файлы: `data/synthetic/schedule_q4_2026_variant3.json` (667), `data/q4_2026_variant3_output.json` (penalties, FEASIBLE), `data/q4_2026_comparison_3.json`.

