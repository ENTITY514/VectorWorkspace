-- 0009: раздел долгосрочного плана для урока КТП (колонка
-- «Разделы долгосрочного плана» в реальном КТП; в редакторе KTPHUB-стиля
-- объединяет уроки в секции). Значение проставляется при генерации плана
-- из ТУП (section.name) и правится учителем в редакторе.

ALTER TABLE ktp_lessons ADD COLUMN section_name TEXT NOT NULL DEFAULT '';